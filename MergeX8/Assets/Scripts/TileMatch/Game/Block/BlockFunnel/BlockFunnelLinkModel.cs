using LayoutData;

namespace TileMatch.Game.Block
{
    public class BlockFunnelLinkModel : BlockModel
    {
        public BlockFunnelLinkModel(Layer.Layer layer, LayerBlock blockData, Block block) : base(layer, blockData, block)
        {
           
        }
    }
}