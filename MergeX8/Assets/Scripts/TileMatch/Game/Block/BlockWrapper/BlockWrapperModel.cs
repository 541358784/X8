using LayoutData;

namespace TileMatch.Game.Block
{
    public class BlockWrapperModel : BlockModel
    {
        public BlockWrapperModel(Layer.Layer layer, LayerBlock blockData, Block block) : base(layer, blockData, block)
        {
        }
    }
}