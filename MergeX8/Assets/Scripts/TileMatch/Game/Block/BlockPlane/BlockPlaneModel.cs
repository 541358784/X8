using LayoutData;

namespace TileMatch.Game.Block
{
    public class BlockPlaneModel : BlockModel
    {
        public BlockPlaneModel(Layer.Layer layer, LayerBlock blockData, Block block) : base(layer, blockData, block)
        {
        }
    }
}