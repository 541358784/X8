using LayoutData;

namespace TileMatch.Game.Block
{
    public class BlockLockModel : BlockModel
    {
        public BlockLockModel(Layer.Layer layer, LayerBlock blockData, Block block) : base(layer, blockData, block)
        {
        }
    }
}